---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: taylorsilva/dcind
    tag: 20.10.7

inputs:
  - name: dp-import-cantabular-dataset

run:
  path: bash
  args:
    - -exc
    - |
      source /docker-lib.sh
      start_docker
      dp-import-cantabular-dataset/ci/scripts/component.sh

caches:
  - path: /var/run/docker.sock
